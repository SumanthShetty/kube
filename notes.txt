https://kubernetes.io/docs/home/ 
Create: 
    kubectl create -f demo.yaml
Apply: 
    kubectl apply -f demo.yaml #This will detect the changes and apply it. 
Pods: 
    In Kubernetes, a pod is the smallest deployable unit. It's a group of one or more containers (like Docker containers) that are always scheduled to run together on the same node and share the same network namespace.
    kubectl run <pod-name> --image=<image-name> # used to create a Pod
    kubectl run demo --image=nginx --dry-run=client -o yaml #used to generate a yaml 
    kubectl run demo --image=nginx --dry-run=client -o yaml > pod.yaml # to get the tempate moved to a file
    kubectl run -h | less # to leverage kubectl --help 
    kubectl get pods #deatils of all the pods
    kubectl get pods -o wide # Additional deatils
    kubectl describe pod <pod name> # gives the whole discription
    #interactions with pods
    kubectl exec -it nginx -- /bin/bash #execution with pods interaction

Deployments: 
    kubectl create deployment test --image=nginx --replicas=3
    kubectl get deployment
    kubectl edit deployment test
    kubectl delete deployment test
    kubectl create deployment test --image=httpd --replicas=3 --dry-run=client -o yaml > deployment.yaml

Namespaces:
    kubectl get namespace
    kubectl edit namespace sumanth
    kubectl delete namespace sumanth
    kubectl create namespace sumanth --dry-run=client -o yaml > namespace.yaml
    kubectl run sumanth-test-ns --image=nginx --namespace=sumanth # creating pod in the specific namespace
    kubectl get pods -n sumanth # Getting the pod infop from the namespace in which the pod was created 

Network/Services
    kubectl port-forward pods/mealie 9000 #port forwarding/mapping  eg: kubectl port-forward pods/mealie-5b64476b9d-wvqs2 8080:80 -n sumanth
    kubectl port-forward services/mealie 9000 #poer forarding can be done at service level
    kubectl expose deployment mealie --port 9000
    kubectl get services -o wide 
   
   Services:
    Cluster IP
    Node port
    Load Balancer
    Load Balancer is not supported in local minikub enviroment external IP comes will be in pending state
    so we can use minikub
    minikube service mealie-service -n sumanth
    or
    minikube service mealie-service -n sumanth --url
